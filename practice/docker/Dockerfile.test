FROM python:3.11-slim

WORKDIR /app
COPY pyproject.toml .

RUN pip install --no-cache-dir . && \
    pip install --no-cache-dir '.[test]' uvicorn

COPY src/ ./src/
COPY tests/ ./tests/

ENV PYTHONPATH=/app

CMD ["pytest", "tests/", "-v", "--asyncio-mode=auto"]
